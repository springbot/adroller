test:
  post:
    - 'gem build *.gemspec'

deployment:
  production:
    branch: current
    commands:
      - echo "Uploading gem files to Gemfury..."
      - for gem in *.gem; do curl -F package=@$gem https://push.fury.io/$GEMFURY_TOKEN/; done
